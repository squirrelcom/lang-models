? ? What are data the relevant
are What the data ? ? relevant
What ? data relevant are the ?
What are the relevant data ? ?
relevant data are What ? ? the
? the are What data ? relevant
? What relevant are data ? the
? are relevant data What the ?
? are What relevant ? the data
? relevant ? data the What are
